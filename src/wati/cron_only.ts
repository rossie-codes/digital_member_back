// import { CronJob } from 'cron';

// // Schedule the function to run every 5 minutes
// const cron_job_only = new CronJob('*/10 * * * * *', async () => {
//   console.log('Running cron_job_only at', new Date().toISOString());
//   try {
//   } catch (error) {
//     console.error('Error running broadcast:', error);
//   }
// });

// export default cron_job_only;
// // Start the cron job
